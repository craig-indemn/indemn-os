[project]
name = "async-deepagents-harness"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "deepagents>=0.1",
    "temporalio>=1.6",
    "langchain-anthropic>=0.3",
    "langchain>=0.3",
]
